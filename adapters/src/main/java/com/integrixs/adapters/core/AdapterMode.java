package com.integrixs.adapters.core;

/**
 * Enumeration of adapter operation modes.
 */
public enum AdapterMode {
    /**
     * Sender mode - adapter sends/pushes data to external systems
     */
    SENDER,
    
    /**
     * Receiver mode - adapter receives/pulls data from external systems
     */
    RECEIVER
}